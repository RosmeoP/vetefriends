{
  "name": "vetefriends",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter \"./apps/*\" dev",
    "dev:web": "pnpm --filter @vetefriends/web dev",
    "dev:api": "pnpm --filter @vetefriends/api dev",
    "build": "pnpm --filter @vetefriends/web build",
    "api:mock": "pnpm --filter @vetefriends/web api"
  }
}
